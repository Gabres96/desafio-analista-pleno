services:
  backend:
    build:
      context: ./backend
      target: builder
    command: npm run start:dev
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules
